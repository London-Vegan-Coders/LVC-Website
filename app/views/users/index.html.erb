<h1>London Vegan Coders Team </h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= profile_image(user) %>
      <%= link_to user.name, user %>
      created
      <%= time_ago_in_words(user.created_at) %> ago
    </li>
  <% end %>
</ul>
